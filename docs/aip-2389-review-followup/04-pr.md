# PR 문구

기존 PR #87에 추가 커밋으로 반영 (별도 PR 아님).

## Commit message

```
fix: limit project name hard blocks to CREATE and clean up allowlist internals

- ProjectReviewHandler의 reserved/allowlist 이름 hard block을 CREATE에만
  적용한다. projects 웹훅은 UPDATE도 가로채고 failurePolicy가 Fail이므로,
  UPDATE까지 거부하면 기존 project의 finalizer 제거가 막혀 삭제가 영구
  terminating으로 교착된다 (legacy reserved-name project, allowlist backstop
  상태 해소 경로 포함).
- 임시 분석 문서 aip-2389-allowlist-scheduling.html 제거.
- NamespaceAllowlistValueEnum dead code 제거(상수 대체),
  PodReconciler의 NamespaceAllowlistResolver 생성자 주입 통일,
  WorkloadControllerFactory 인라인 팩토리 생성 제거,
  ClusterRole(Binding)Reconciler의 NamespaceNameResolver 필드화.
- 테스트: allowlist-우선 순서(PodReviewHandler/DeploymentReviewHandler/
  WorkloadControllerReconciler)와 UPDATE hard block 통과 회귀 테스트 추가.
```

## PR 본문 추가 문단 (기존 PR #87 설명 갱신용)

### 이름 충돌 가드 — hard block 범위 (리뷰 반영)

hard block(reserved/allowlist 이름 거부)은 **CREATE에만** 적용됩니다. Project 삭제는 finalizer 제거 UPDATE를 거치는데, 이 웹훅은 `failurePolicy: Fail`이라 UPDATE까지 거부하면 삭제가 영구 terminating으로 교착되기 때문입니다. UPDATE는 기존 권한 검사(system admin / project-manager)를 그대로 따릅니다.

## Test plan

- [x] `./gradlew test` 전체 통과 (27클래스 144테스트)
- [x] reserved/allowlist 이름 CREATE가 system admin에게도 409 거부
- [x] 동일 이름 UPDATE는 hard block 통과 (finalizer 제거 시나리오)
- [x] allowlist 네임스페이스: 제외 라벨보다 allowlist 우선 (웹훅 2종 + 워크로드 리컨실러)
- [ ] 실클러스터 스모크: allowlist backstop 상태에서 Project 삭제가 완료되는지 확인
