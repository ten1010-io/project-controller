spring:
  application:
    name: project-controller
  profiles:
    active: "production"
  web:
    resources:
      add-mappings: false

server:
  forward-headers-strategy: framework

app:
  k8s:
    verify-ssl: false
    kube-config:
      mode: in_cluster
      # kube-config-path: /users/user/.kube/config
  aipub:
    enabled: false
    server-url: "https://aipub.example.com"
    verifying-ssl: false
    username: "testuser"
    password: "asdasdasd"
    reserved-namespace: "aipub, project-controller"
