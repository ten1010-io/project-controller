spring:
  application:
    name: project-controller
  profiles:
    active: "production"
  web:
    resources:
      add-mappings: false

server:
  forward-headers-strategy: framework

app:
  k8s:
    verify-ssl: false
    kube-config:
      mode: file
      kube-config-path: /users/joon/.kube/config
  aipub:
    enabled: false
    server-url: "https://aipub.cluster6.idc1.ten1010.io"
    verifying-ssl: false
    username: "testuser"
    password: "asdasdasd"
    reserved-namespace: "aipub, project-controller"
