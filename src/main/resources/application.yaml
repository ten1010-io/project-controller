spring:
  application:
    name: project-controller
  profiles:
    active: "production"
  web:
    resources:
      add-mappings: false

server:
  forward-headers-strategy: framework

app:
  k8s:
    verify-ssl: true
    kube-config:
      mode: file
      kube-config-path: "/Users/daekwonpark/.kube/config"
#      mode: in_cluster
  aipub:
    enabled: false
    server-url: "https://aipub.clutser6.idc1.ten1010.io"
    verifying-ssl: false
    username: "testuser"
    password: "asdasdasd"
    reserved-namespace: "aipub, project-controller"
